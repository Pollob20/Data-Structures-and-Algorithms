#include <stdio.h>

int recursiveLinear(int arr[], int n, int key, int index) {
    if(index >= n)
        return -1;

    if(arr[index] == key)
        return index;

    return recursiveLinear(arr, n, key, index + 1);
}
