#include <stdio.h>
#include <string.h>

struct Student {
    int id;
    char name[50];
};

int searchByName(struct Student s[], int n, char target[]) {
    for(int i = 0; i < n; i++) {
        if(strcmp(s[i].name, target) == 0)
            return i;
    }
    return -1;
}
