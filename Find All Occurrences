#include <stdio.h>

void findAll(int arr[], int n, int key) {
    int found = 0;
    for(int i = 0; i < n; i++) {
        if(arr[i] == key) {
            printf("Found at %d\n", i);
            found = 1;
        }
    }
    if(!found)
        printf("Not Found");
}
